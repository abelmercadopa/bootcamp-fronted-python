# bootcamp fronted g30
este repositorio nos sirve para aprender el flujo de trabajo de git y github
git config --global user.name "nombre"
git condig --global user.email "corre@ejemplo.com2
### comandos basicos:
* git init
* git status : para ver el estado de mostrar repositorp
* git add .
* git commit -m "subir archivos de la primera semana"

